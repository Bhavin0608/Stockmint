# Adding the base URL for the API to be used in the frontend application. This allows the frontend to make requests to the backend server running on localhost at port 5000.
# Why VITE_ as a prfix because it exposes the env variables to frontend code.
VITE_API_URL=http://localhost:5000/api